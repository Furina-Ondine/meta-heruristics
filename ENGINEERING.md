# 工程规范

## 文档权威性

- 本文是持续有效的工程契约；`README.md` 只提供摘要。
- [架构概览](docs/architecture/overview.md)只报告实现现状；[ADR 索引](docs/decisions/README.md)及其中 `Accepted` 的记录解释选择原因。
- [`docs/specs/`](docs/specs/README.md) 中 Approved 或 Implemented 的 Spec 定义对应功能的公共意图和行为；Plan、Tasks 和 Verification 只记录实现转换与证据。
- `docs/superpowers/` 保存旧设计和实施过程，只作为历史档案，不承载新的权威功能规格。
- 发现实现、规格、架构概览和 ADR 不一致时，先报告冲突；由用户决定修正实现、修订 Spec 或用新 ADR 替代决策后，同步更新本文和架构概览。

## 项目边界

- `Metaheuristics.Core` 定义稳定契约且不引用其他仓库项目；`Algorithms` 和 `Experiments` 只依赖 `Core`，彼此不依赖。
- 既定依赖图如下，新增引用必须保持单向：

  ```text
  Algorithms   ──→ Core
  Experiments  ──→ Core
  Examples     ──→ Core + Algorithms + Experiments
  Tests        ──→ Core + Algorithms + Experiments
  Benchmarks   ──→ Core + Algorithms
  ```

- 论文业务模型、实验专用逻辑和领域解码不进入运行时包；具体算法不得反向污染核心抽象。

## API 与扩展

- 优先使用强类型接口、显式依赖和组件组合；不引入全局字符串注册中心、服务定位器或按字符串类型名恢复组件的机制。
- Core 只定义运行生命周期所需的最小稳定协议；Objective、Constraint、Initializer、Repair、Stopping Condition 和 Optimizer 等策略拥有自己的数据和行为，由调用方选择并显式组装。
- Core 只验证维持自身协议所必需的不变量；能在构造阶段验证一次的配置不得在热路径重复验证，策略拥有的数据不由 Core 重复判断或静默修复。
- 用户选择并装配问题、算法和策略，库管理运行生命周期；批量实验使用强类型配置和 RunGroup 工厂创建隔离实例，不由库解释任意配置或按类型名构造组件。
- 只为已进入路线图的表示类型、目标类型和计算后端建立抽象。
- 一个运行概念只能有一个权威所属层；职责迁移时默认删除失去独立价值的旧类型、入口、转发层和兼容壳。只有存在已确认的真实消费者、保留期限和删除条件时才能例外。

## 运行状态与随机性

- 强类型 Config 保存算法定义和不可变参数；Factory 为每个 RunGroup 创建独立的 `IOptimizer`。`IOptimizer` 直接拥有种群数组和临时缓冲区，可在该 Group 的顺序 run 之间复用物理存储，但每次 run 必须完整重置逻辑状态。
- `IOptimizer` 不保证线程安全，不得跨 RunGroup 共享，也不得在运行异常后继续复用；通用接口只持有托管资源，不要求实现 `IDisposable`。
- 每个 RunGroup 使用独立 Problem 和 Optimizer；不同 Group 只能共享调用方明确提供的不可变底层数据。
- 禁止全局随机流（包括 `Random.Shared`）、当前时间播种和跨运行共享逻辑状态；每个 run 使用由实验计划确定的独立 seed 和 `Random` 实例，其随机序列必须不受 RunGroup 拆分、调度顺序或并发度变化影响。
- `IStoppingCondition` 必须可重入，不得保存 run 级可变状态；同一实例允许被多个运行并发调用。
- 相同库版本、运行时和执行设置下，相同种子必须产生相同结果。
- 池化资源必须明确所有权、清理和释放规则。

## 数值正确性

- 明确验证构造配置、边界定义、目标值和约束结果；无效范围和容差不得静默传播。目标值接受有限值与正负 Infinity、拒绝 `NaN`；约束违背量接受非负有限值与 `+Infinity`、拒绝负值和 `NaN`。Infinity 的比较、停止与统计语义见 [ADR-0015](docs/decisions/0015-ordered-extended-evaluation-values.md)。
- 候选位置的合法性由调用方组合的 `ICandidateInitializer` 与 `ICandidateRepair` 负责。算法在初始化位置后及每次修改位置后必须调用 Repair；Core 不验证候选位置，也不向算法公开变量上下界。
- 内置 Repair 的特殊数值语义必须由文档和测试定义：`NaN` 保持不变，`-Infinity`/`+Infinity` 分别表示无下界/无上界；默认 Clamp 使用 `[0, 10]`。`DoNothing` 只能作为调用方明确承担后果的风险选择。
- 等式约束使用显式绝对容差和相对容差；约束判定、候选比较和候选修复保持独立职责。
- 数值异常的公共行为必须由文档和测试定义。

## 性能

- 先保证正确性、边界清晰和可测试性；热路径优化必须有 BenchmarkDotNet 数据或分配分析依据。
- 性能类修改的 Verification 必须简短报告改动部分的加速比与代表性整体任务的端到端加速比，并记录基线、运行环境、基准配置、输入规模和分配；无法构造有意义的整体任务基准时，必须说明原因及替代证据。加速比按 `基线耗时 / 修改后耗时` 表示，不得将单机、单运行时或单输入场景外推为通用性能承诺。
- `Span<T>`、池化和并行化必须保留清晰且正确的基础路径；性能改动不得破坏确定性、资源安全和公开语义。
- 首版只提供单点评估契约，不规定算法的 AoS、SoA 或其他种群布局；批量评估必须经实际算法、布局转换和约束处理的端到端基准证明后才能重新进入公共 API。
- 批量实验通过 `RunGroupCount` 显式限制一个 Case 的 Optimizer 数量，并只在 Group 内顺序复用算法工作区；异常后的 Optimizer 不继续复用。
- 不为首版引入远程执行、集群调度或 GPU 抽象。

## 测试和质量

- 新增或改变公共行为时先定义可观察契约，并由自动化测试覆盖；核心契约覆盖正常、边界、无效输入和取消行为。
- 算法测试覆盖标准测试函数、固定种子复现、并发隔离和数值安全；性能承诺必须有对应基准。
- 合入前至少执行 Release 配置的 restore、build 和 test；公共 API、示例和文档随行为变化同步更新。

## 文档和注释

- README 负责项目判断和导航；User Guide 按用户任务教学；API Overview 只提供入口地图；Developer Guide 按扩展任务说明架构约束；架构概览只报告当前状态；ADR 只解释决策原因。
- 一项完整契约只在一个权威位置维护，其他文档提供与当前读者任务相关的摘要和链接，不复制整段规则。
- 参数、返回值、异常、所有权、生命周期和成员级特殊语义以源码 XML 注释为唯一内容来源，并由生成式 API Reference 呈现。
- 简单属性允许只描述所表示的值；其他公共符号的注释必须提供签名之外的用途、约束、所有权、副作用、生命周期、线程安全、数值语义或异常信息。
- 注释必须能由实现、测试、Approved Spec 或 Accepted ADR 支持；陈旧或失真的 XML 注释按公共 API 缺陷处理。内部注释说明原因、约束和非显然处理，不逐句复述代码。

## 代码风格

- 遵循标准 .NET 命名、API 设计和异步命名约定；保持 nullable、编译器分析、确定性构建和警告视为错误。
- `.editorconfig` 是可机械执行的格式与风格来源；初期不引入沉重的 StyleCop 规则集。
- 注释说明原因、约束和不明显的数值处理，不逐句复述代码。

## 版本发布

- `Metaheuristics.Core`、`Metaheuristics.Algorithms` 和 `Metaheuristics.Experiments` 使用同一版本号并锁步发布，即使某个包没有代码变化也必须发布同一版本。
- 采用 SemVer 2.0.0：`MAJOR` 表示不兼容公共 API 或行为变化，`MINOR` 表示向后兼容的新功能，`PATCH` 表示向后兼容的缺陷修复；预发布版本使用标准预发布标识。
- `1.0.0` 前的破坏性变化至少提升 `MINOR` 并在发布说明中记录；达到 `1.0.0` 后按 SemVer 提升 `MAJOR`。纯文档或仓库维护不必产生包版本。

## 决策变更

- 重大架构变化，以及改变项目或包的依赖方向、公共执行模型、组件构造职责、状态所有权、候选/目标/约束/比较语义、计算后端、跨进程能力、可复现性承诺、性能策略或发布规则，必须先新增或替代 ADR。
- 对既定路线图作实质性的顺序或范围调整也必须有 ADR；局部实现、内部重构和不改变公共行为的缺陷修复无需新增 ADR。
- 新增、删除或改变公共 API、可观察行为、职责归属、策略或执行抽象、状态、随机性、数值语义、热路径性能、兼容策略，或跨两个以上运行时项目的变更，必须进入完整 SDD。
- 完整 SDD 按 Spec、澄清、用户批准、Plan、用户批准、Tasks、实现和 Verification 推进；存在影响公共行为的未决问题时不得批准，实施中出现未批准的设计判断时必须停止并退回 Spec 或 Plan。
- 只有不改变公共 API、可观察行为、职责、抽象、数值、状态、并发和确定性契约，且已有权威资料明确期望的局部修改，才能采用经用户批准的轻量设计。隐藏复杂度只能升级流程。
